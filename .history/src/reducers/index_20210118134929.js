import {FETCH_MOVIES } from '../actions/types';

const initialState = {
    movies: [],
    query: '',
    showBanner: false,
    loading: 
}


export default function (state = initialState, action){
    
}