import React, { Component } from 'react'
import { connect } from 'react-redux';
import {fetchUser } from '../actions';


 class UserHeader extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default connect(null, fetch) (UserHeader);